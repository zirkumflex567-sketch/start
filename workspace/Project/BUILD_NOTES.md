# Build Notes

## Target Platform
- Primary: Windows (standalone).
- Secondary: macOS once core loop is stable.

## Build Settings Reminders
- Use URP 2D renderer and linear color space.
- Disable splash screens where allowed.
- Verify Force Text serialization before committing assets.
